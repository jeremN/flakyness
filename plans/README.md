# Implementation Plans

Generated by the improve skill on 2026-07-10 at commit `0f8b0cc`. Execute in
the order below unless dependencies say otherwise. Each executor: read the
plan fully before starting, honor its STOP conditions, and update your row
when done.

Selection provenance: full-repo audit (correctness, security, performance,
tests, tech debt, deps, DX, docs, direction) at `standard` depth; findings
vetted against the code by the advisor; the maintainer selected all four
proposed bundles (top-5 core, flakiness refactor, quick wins, hardening
extras) and deferred all direction/feature spikes.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Make the parser ingest real Playwright JSON reports | P1 | M | — | DONE (merged via PR #36, commit `69eee5c`) |
| 002 | Build Docker images from the lockfile, on pnpm 11 + Node 24 | P1 | M | — | DONE (merged via PR #37, commit `4b426b9`) |
| 003 | Make the integration docs match the API | P1 | S | 001 (for e2e verify only) | DONE (merged via PR #39, commit `d088546`; e2e-verified 201 with real fixture) |
| 004 | Make the tests exercise the shipped code | P1 | M | — | DONE (merged via PR #40, commit `f7764f3`; includes lazy DB init in `db/index.ts`) |
| 005 | API quick fixes (double-decode, hono bump, limits, cascade delete, dup route, dead scripts) | P2 | M | — | DONE (merged via PR #44, commit `28a19fc`) |
| 006 | Make `updateFlakyTests` transactional, batched, `ignored`-preserving | P2 | M | **004 (hard)** | DONE (merged via PR #42, commit `44bc734`) |
| 007 | Dashboard vitest baseline (runner + data-layer coverage) | P2 | M | — | DONE (merged via PR #41, commit `8d79a50`; test script is sync-first for fresh checkouts) |
| 008 | Dashboard graceful degradation on API failure | P2 | M | **007 (hard)** | DONE (merged via PR #47, commit `b5552b3`; also fixed pre-existing Chart.svelte SSR crash its smoke test exposed) |
| 009 | Dashboard quick wins (modular ECharts, dead Tailwind config, CSP) | P3 | S | — | DONE (merged via PR #46, commit `400bec7`; largest chunk 1,127→502 kB) |
| 010 | Pin GitHub Actions to commit SHAs + provenance | P3 | S–M | 002 (soft — same files, land 002 first) | DONE (merged via PR #38, commit `399d811`) |
| 011 | Root AGENTS.md + CLAUDE.md | P3 | S | none (write last so the command table reflects reality) | DONE (merged via PR #48, commit `7609d55`) |

### Batch 2 — direction features (planned 2026-07-10 at commit `7609d55`)

Provenance: `/improve next` direction audit after batch 1 completed; six core
direction findings + two companions presented; the maintainer selected ALL
EIGHT. Recommended execution waves (see Dependency notes):
**wave 1**: 012 + 019 (parallel) → **wave 2**: 013 + 014 (parallel) →
**wave 3, strictly serial**: 015 → 016 → 017 → 018.

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 012 | Ignore/mute flaky tests (PATCH route + dashboard action) | P2 | M | — (lands the dashboard write path others may reuse) | DONE (merged via PR #50, commit `111cdcf`) |
| 013 | Per-project flakiness thresholds | P2 | M | migration-serial: first of 013/015/016 | DONE (merged via PR #52, commit `7052053`) |
| 014 | Surface the /analysis endpoint in the dashboard | P2 | M | 012 (soft — shared `lib/api.ts`/nav files) | DONE (merged via PR #51, commit `e8504f2`) |
| 015 | Persist Playwright tags & annotations | P3 | M | migration-serial: after 013 | DONE (merged via PR #53, commit `6d37baf`) |
| 016 | Webhook notifications on flaky transitions | P2 | M–L | 013 (soft — admin PATCH pattern); migration-serial: after 015 | DONE (merged via PR #54, commit `2c25fce`; executor fixed a Zod v4 refine crash in the planned validation) |
| 017 | JUnit XML ingestion | P2 | M–L | 015 (soft — shared parser/reports files); lockfile-serial: before 018 | DONE (merged via PR #55, commit `b593a9b`) |
| 018 | Prometheus /metrics endpoint | P3 | M | lockfile-serial: after 017 | DONE (merged via PR #56, commit `1daeb46`; executor extracted `extractBearerToken`/`tokensMatch` from `adminAuth` so `/metrics` shares the constant-time comparison rather than re-implementing it) |
| 019 | Self-host the Inter font | P3 | S | — (low-conflict, anytime) | DONE (merged via PR #49, commit `d0b15b7`) |

**Batch 2 complete (2026-07-13):** all 8 direction plans landed via PRs #49–#56.
Follow-up noted during 018 review (not blocking, no plan yet): `/metrics` carries
no rate limiter, matching `/health` — it does no DB work before auth, so an
unauthenticated flood gets a cheap 404/401 with no amplification. If one is ever
added, `adminRateLimit` (5/min) would break a 15s scrape interval; `apiRateLimit`
(100/min) is the only safe choice.

### Batch 3 — direction (planned 2026-07-13 at commit `1642dc4`)

Provenance: `/improve next` direction audit after batch 2 completed. Six
direction findings presented (D1–D6); the maintainer took the advisor's
recommendation — D1 + D3 as the core pair, D6 riding along. D2 and D5 were
deliberately deferred (see "Direction findings deferred" below).

The through-line: after batch 2, Flackyness is a capable **observability** tool
whose knowledge never leaves the dashboard. 020 is the first step from
*observe* to *act*.

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 020 | Quarantine list for CI (`GET /projects/:id/quarantine`) | P2 | M | — (012's `ignored` status already landed) | DONE (merged via PR #57, commit `4e29610`; executor extracted `buildGrepInvert` so the "empty muted set ⇒ empty pattern" invariant is a pure no-DB unit test) |
| 021 | Per-project data retention + admin prune endpoint | P2 | M | migration-serial: generates `0006` | DONE (merged via PR #63, commit `7899671`; migration `0006`. Reviewed adversarially: confirmed pruning project A leaves project B's runs/results untouched, `?confirm=1` does not delete, and `flaky_tests` — including `ignored` mutes — survives a confirmed prune) |
| 022 | Refresh `.agent/CONTEXT.md` to match the codebase | P2 | S | — (docs only, no file overlap) | DONE (merged via PR #58, commit `353e6789`; one REVISE round — executor had left two bare `docker compose up -d` calls in CONTEXT.md, one commented "Start PostgreSQL only", which is false) |

**Batch 3 complete (2026-07-13):** all 3 plans landed via PRs #57, #58, #63.

### Batch 4 — TypeScript 7 (planned 2026-07-13 at commit `74468af`)

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 023 | Adopt TS 7 in the API; pin the dashboard on TS 6 until svelte-check catches up | P2 | S–M | — | DONE (merged via PR #64, commit `bd78380`) |

**Why it's a plan and not a merge:** Dependabot opened #60/#61/#62 bumping
`typescript` 6.0.3 → 7.0.2. They disagree with each other, and the disagreement is
the finding — **#61 (api only) passes every check**, while **#62 (dashboard) and
#60 (both) crash `Type Check`**.

**Correction to an earlier claim in this file and in `AGENTS.md`:** the blocker is
NOT the `ignoreDeprecations: "6.0"` bridge in the root tsconfig. PR #61 proves the
API typechecks clean under TS 7 with that bridge still in place. The real blocker is
**`svelte-check` 4.x**, which reads `ts.default.sys.useCaseSensitiveFileNames` — a
CommonJS shape the native TS 7 rewrite no longer exposes — and so crashes on startup.
Its peer range (`typescript: ">=5.0.0"`) is simply wrong, so nothing stops the
install. That bug is upstream and cannot be fixed in this repo; plan 023 takes the
half that works (the API) and fences the half that doesn't. Plan 023 also corrects
the wrong sharp-edge note in `AGENTS.md`/`.agent/CONTEXT.md`.

**Batch 4 complete (2026-07-13):** plan 023 landed via PR #64. `apps/api` now
resolves TypeScript **7.0.2**, `apps/dashboard` **6.0.3**; svelte-check still runs
(398 files, 0 errors). `ignoreDeprecations` was empirically confirmed removable and
is **gone** from the root tsconfig.

- **Outstanding operator action**: close Dependabot PRs **#60, #61, #62** as
  superseded by #64. (#61 was correct but only did the API half; #60/#62 will keep
  failing until svelte-check supports TS 7. The new dashboard ignore rule stops
  #62's kind from being re-opened weekly.)
- **The dashboard TS pin is temporary.** Lift it when svelte-check ships TS 7
  support: bump the dashboard's TypeScript and delete the `typescript` majors
  ignore from the dashboard's Dependabot entry.
- **Follow-up noticed during 023 (no plan yet)**: `apps/api`'s build is
  `tsup src/index.ts --format esm` — **no `--dts`**, no tsup config — so no `.d.ts`
  is ever emitted despite `declaration: true` in the root tsconfig. Harmless for an
  app with no consumers, but it means the long-standing tsconfig comment blaming
  "baseUrl injected by tsup's d.ts builder" described something that does not happen.
- **New Dependabot maintenance obligation**: the npm entry now lists `/` and
  `/apps/api` explicitly instead of the `/apps/*` glob (a glob would overlap the
  dashboard entry and double-open its PRs). **A new app under `apps/` must be added
  to that list by hand, or it silently gets no dependency updates.**

**Batch 2 complete (2026-07-13):** all 8 direction plans landed via PRs #49–#56.

**Batch 1 complete (2026-07-10):** all 11 plans landed via PRs #36–#42, #44, #46–#48.
Notable extras found during execution: pre-existing `Chart.svelte` SSR crash
(fixed in #47); `pnpm db:migrate` requires root `.env` to exist (documented in
AGENTS.md); GETTING_STARTED.md's bare `docker compose up -d` dev flow can
port-clash without the override file (AGENTS.md documents the safe form —
a GETTING_STARTED.md touch-up is a candidate follow-up). Direction findings
(see below) can now be re-proposed per the note in "Findings considered and
rejected".

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **006 requires 004**: the `updateFlakyTests` rewrite must run against plan
  004's integration suite; without it the refactor has no regression net.
- **008 requires 007**: 008 rewires error behavior and updates tests that 007
  creates; without the harness its changes are unverifiable.
- **003's Step 6 (end-to-end example check) requires 001**: the upload example
  only succeeds once the parser accepts real reports. The doc edits themselves
  can land anytime.
- **010 after 002**: both edit the workflow docker jobs; 002 changes
  `context:`/`file:`, 010 changes `uses:` refs — landing 002 first avoids
  conflicts.
- **011 last in the batch**: its command table must describe what actually
  landed (e.g. the dashboard `test` script only exists after 007; the missing
  `tailwind.config.js` note assumes 009).
- 001 + 003 together restore the product's core promise (real CI reports
  ingest and the documented onboarding path works) — treat them as one
  milestone.

### Batch 2 dependency notes

- **Migration serialization (hard)**: 013, 015, 016 each generate a drizzle
  migration; `apps/api/drizzle/` is at `0002_*` — parallel worktrees would
  all mint `0003_*` and collide. Land one at a time (013 → 015 → 016), each
  regenerating on top of the previous.
- **Lockfile serialization (hard)**: 017 (`fast-xml-parser`) and 018
  (`prom-client`) both edit `pnpm-lock.yaml` — land 017 first.
- **012 before 014 (soft)**: both touch `apps/dashboard/src/lib/api.ts` and
  nav-adjacent files; batch-1 taught us adjacent-line edits conflict across
  worktrees. 012 also establishes the dashboard's server-side
  `ADMIN_TOKEN`/form-action write pattern.
- **013 before 016 (soft)**: 016 extends the admin `PATCH /projects/:id`
  route that 013 creates (016 carries a fallback if 013 hasn't landed, but
  ordering avoids duplicate route scaffolding). Both also touch
  `services/flakiness.ts`.
- **015 before 017 (soft)**: 017's JUnit parser must emit whatever
  `ParsedTestResult` looks like — landing 015 first fixes that interface
  before 017 implements against it. Both touch `routes/reports.ts`.
- **019 anytime**: dashboard styling files only; no overlap with anything.
- If plan 013 lands, plan 014's displayed defaults should be read from the
  analysis RESPONSE (the API echoes resolved values) — noted in both plans'
  maintenance sections.

### Batch 3 dependency notes

- **020 and 021 both edit `docs/API.md`** (soft): each adds an endpoint section.
  Land them serially, or expect a trivial adjacent-line conflict between
  worktrees — batch 1 taught us adjacent-line edits do collide.
- **021 is migration-serial**: it generates `0006_*`. Nothing else in this batch
  has a migration, so it can go any time — but if a future plan is dispatched in
  parallel and also generates one, they will both mint `0006` and collide.
- **022 is fully independent**: `.agent/CONTEXT.md` + `docs/GETTING_STARTED.md`
  only, zero overlap with 020/021. Safe to run in parallel with either.
- **020 before any GitHub-Action work** (D2, unplanned): a PR comment is far more
  useful when it can also say "these were auto-retried", which needs 020's
  endpoint to exist first.

## Direction findings deferred (batch 3 audit, 2026-07-13)

Grounded and still open — the maintainer chose not to plan them *yet*. Evidence
is recorded here so the next audit doesn't re-derive them from scratch.

- **D2 — CI-native feedback: a GitHub Action that comments on the PR.** The repo
  ships only `.gitlab-ci.yml.example` (an upload snippet); plan 016's webhooks
  push outward, but nothing posts back into the PR where the decision is made.
  Effort M–L. **Blocked on 020 by preference, not by code** — the comment is much
  more valuable once a quarantine/retry list exists.
- **D4 — Flake-rate history / regression detection.** `flaky_tests` stores only
  the *current* `flakeRate` (no time series), so "is this test degrading?" is
  unanswerable and 016's webhooks fire only on `active`↔`resolved` transitions. A
  snapshot table would enable trend arrows and "degrading" alerts. Effort M–L.
  Deferred as possibly premature: the same answer may be computable on the fly
  from `test_results` without a new table — settle that before building one.
- **D5 — Dogfood: give the flaky-test tracker its own Playwright E2E suite.**
  Confirmed zero E2E tests (no `playwright.config`, no spec files); `.agent/CONTEXT.md`
  has listed this as pending since June. Running an E2E suite in CI *and ingesting
  its own report into Flackyness* is coverage, a live validation of the ingest
  path, and the most honest demo the project could have. Effort M. Deferred as
  non-core, not as a bad idea — worth picking up any time.

## Findings considered and rejected

(Recorded so they aren't re-audited.)

- **Trend endpoint aggregates per-day in JS instead of SQL GROUP BY**
  (`projects.ts:198-243`): bounded by the 90-day clamp; rewrite risks subtle
  UTC/zero-fill semantic drift for marginal gain at current scale. Not worth
  doing now; revisit if per-project run volume grows very large.
- **No HTTP caching on read endpoints / SSR loads**: real but low-value at
  current traffic; bounded staleness policy is a product decision. Deferred.
- **`getClientIp` reaches the Node socket via untyped casts**
  (`rate-limit.ts:20-33`): works with the current `@hono/node-server`;
  investigate-only. If the file is touched for other reasons, prefer
  `getConnInfo` from `@hono/node-server/conninfo`.
- **Leftover local `packages/shared/` dist/node_modules cruft**: untracked by
  git; delete locally whenever (`rm -rf packages/shared`). No plan needed.
- **Unauthenticated read APIs, in-memory rate limiter, in-memory flakiness
  aggregation**: documented, intentional tradeoffs in `.agent/CONTEXT.md` —
  by design at concept stage, not findings.
- **Dropping `credentials: true` from the API CORS config**: defense-in-depth
  idea surfaced during the audit (auth is Bearer-based, not cookie-based);
  behavior change needs the maintainer's explicit call. Raise in review of
  plan 005's hono bump if desired.
- **Direction findings** (Prometheus `/metrics`, JUnit ingestion, flaky-
  transition notifications, persisting Playwright tags, surfacing the
  `/analysis` endpoint in the UI): grounded options, deliberately deferred by
  the maintainer until the foundations above land. Evidence is in the audit
  summary; re-propose after plans 001–008 are DONE.
